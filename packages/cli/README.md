# @multi-profile-provider/cli

CLI and launcher for multi-profile-provider runtime isolation.

Primary commands: `mpp run` and `opencode-mpp`.
See repository root `README.md` for complete command and release documentation.
