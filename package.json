{
  "name": "multi-profile-provider",
  "version": "0.1.1",
  "private": true,
  "description": "OpenCode provider profile switcher with isolated data roots",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "pretest": "node -e \"require('node:fs').mkdirSync('coverage/.tmp',{recursive:true})\"",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint . --ext .ts",
    "prepare": "npm run typecheck",
    "release:pack:dry-run": "npm pack --dry-run --workspace @multi-profile-provider/core && npm pack --dry-run --workspace @multi-profile-provider/cli && npm pack --dry-run --workspace @multi-profile-provider/opencode && npm pack --dry-run --workspace @multi-profile-provider/opencode-plugin && npm pack --dry-run --workspace multi-profile-provider-opencode-plugin"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.15.17",
    "@vitest/coverage-v8": "^2.1.9",
    "eslint": "^10.3.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.59.2",
    "vitest": "^2.1.9"
  }
}
