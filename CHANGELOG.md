# Changelog

## 0.1.0
- Initial MVP scaffold for profile creation/list/select/rename/delete.
- Isolated data root policy and restart fallback flow.
- Removed legacy shim lifecycle commands (`mpp install`/`mpp uninstall`) in favor of explicit launchers (`mpp run`, `opencode-mpp`).
- Added npm release-readiness metadata and dry-run release guidance (no publish performed).
